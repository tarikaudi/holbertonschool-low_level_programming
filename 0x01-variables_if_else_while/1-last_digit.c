#include <stdlib.h>
#include <time.h>

/**
 * main - compare a random number if it is negative positive or 0
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;
	int LastDigit = n % 10;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	
	if (
	return (0);
}
