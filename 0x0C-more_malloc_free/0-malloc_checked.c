#include "main.h"

/**
**malloc_checked - main main
*@b: vari
*Return: 0 as always
*/

void *malloc_checked(unsigned int b)
{
	void *arre;

	ar = malloc(b);
	if (ar == NULL)
	{
		exit(98);
	}

	return (ar);
}
