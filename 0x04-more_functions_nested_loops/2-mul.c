#include "main.h"

/**
 *mul - adds two integers
 *@i: variable stores another
 *@b: variable stores one
 *Return: return return i + b
*/
int mul(int i, int b)
{
	int c;

	c = (i * b);
	return (c);

}
